# Python - import & modules

This project introduces how to import functions and variables from other
Python files, how to use command line arguments, and how Python modules work.

## Learning Objectives

By the end of this project, you should be able to explain:

- How to import functions from another file
- How to use imported functions
- How to create and use modules
- How to use built-in function `dir()`
- How to prevent code from being executed when imported
- How to use command line arguments with `sys.argv`

## Requirements

- Allowed editors: `vi`, `vim`, `emacs`
- All files interpreted on Ubuntu 20.04 LTS
- Python version: Python 3
- All Python files must:
  - Start with `#!/usr/bin/python3`
  - End with a new line
  - Follow PEP 8 style (`pycodestyle`)
  - Be executable

## Files

| File | Description |
|------|-------------|
| `0-add.py` | Imports and uses the add function |
| `1-calculation.py` | Simple calculator using imported functions |
| `3-infinite_add.py` | Adds all command line arguments |
| `4-hidden_discovery.py` | Prints names defined in a module |
| `5-variable_load.py` | Prints a variable from an imported file |
| `README.md` | Project documentation |

## Usage

Make scripts executable:

```bash
chmod u+x *.py

